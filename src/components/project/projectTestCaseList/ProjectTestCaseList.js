const ProjectTestCaseList = (props) =>{
    return(
        <div className={"projectTestCase"}>
            <h2>Сценарии тетсирования тестирования : {props.projectId}</h2>
        </div>
    )
}

export default ProjectTestCaseList;